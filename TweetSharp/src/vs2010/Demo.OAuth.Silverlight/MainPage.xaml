<UserControl x:Class="Demo.Silverlight.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    mc:Ignorable="d" d:DesignWidth="640" d:DesignHeight="480">
  <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition Height="40"></RowDefinition>
        </Grid.RowDefinitions>
        <HyperlinkButton 
            Grid.Row="0"
            x:Name="LoginButton"             
            Content="Login with OAuth"
            Width="200" Height="40"
            TargetName="_blank"
            HorizontalAlignment="Center" 
            VerticalAlignment="Center">
        </HyperlinkButton>
        <StackPanel
            x:Name="PinControl"
            Grid.Row="1"
            Orientation="Horizontal" 
            HorizontalAlignment="Center" 
            VerticalAlignment="Center"
            Height="40"
            Visibility="Collapsed">
            <TextBlock Text="Enter your PIN:" />
            <TextBox x:Name="LoginPin" Width="200"></TextBox>
            <Button x:Name="SubmitPin" Width="60" Content="Submit" Click="SubmitPin_Click"></Button>
        </StackPanel>            
  </Grid>
</UserControl>
